# .env.example - copy to .env and fill with your project values
# Notes:
# - Environment variables used by Vite must start with VITE_.
# - To use locally, duplicate this file as `.env` in the project root.
# - Get Firebase config values from your Firebase project settings.
# - Get API keys (Gemini/OpenAI/etc.) from the provider dashboards.

# Firebase configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Gemini / Google Generative AI API key (used across many components)
# Example: the key string provided by the @google/generative-ai provider or your service token
VITE_GEMINI_API_KEY=

# RapidAPI key for the job search (JSearch) integration
VITE_RAPIDAPI_KEY=

# Vapi (voice interview) API key
# Used by @vapi-ai/web. Provide the public client key or token if required by Vapi setup.
VITE_VAPI_API_KEY=

# Optional: specify the Vite dev server port (if you want to override default)
# VITE_PORT=5173

# Optional: toggles or environment markers
# NODE_ENV=development

# End of file
